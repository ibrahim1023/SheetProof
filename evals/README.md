# Evals

This folder will hold behavior evaluation assets for non-deterministic flows.

Planned structure:

- `datasets/`
- `rubrics/`
- `results/`

Rules:
- add regression cases for every discovered failure mode
- keep deterministic checks in tests; keep behavior quality checks in evals
