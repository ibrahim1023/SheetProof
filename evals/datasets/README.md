# Eval Datasets

Store evaluation inputs and expected behavior definitions.

Rules:
- include regression cases for every discovered failure mode
- keep deterministic test fixtures in `tests/fixtures/`; keep behavior eval datasets here
- version dataset changes with rationale in commit message or ADR when significant
