# Eval Rubrics

Store scoring rubrics and pass/fail gate definitions for non-deterministic behavior.

Rules:
- define evaluator limitations
- prefer explicit binary gate checks for release criteria
- do not use open-ended "is this good" prompts
